# Comic Avails — Handoff (end of session 2026-07-03)

**Resume target: Part 4 of the build guide (the app — browse + pull list UI).**
Read §0 first for the one prerequisite that gates Part 4.

---

## 0. START HERE (resume checklist)

Part 4 builds the reader UI whose default view is "This Week's FOC" — **it needs
real items in the database**, which means Part 3 (ingest) must have run at least
once. That is currently blocked. So the resume order is:

1. **Fix `.env.local`** — two keys are still placeholders (see §2, B1).
2. **Apply the migration** to Supabase (see §2, B2).
3. **Run the DC ingest** and clear the 403 question (see §2, B3):
   ```
   npx tsx scripts/ingest-dc.ts https://www.comicreleases.com/2026/06/dc-september-2026-solicitations/
   ```
   Do the trust check (build guide §3.4).
4. **Then Part 4** (see §4).

> If you'd rather build the UI before data exists, you *can* — Part 4 against an
> empty DB just renders empty states. But you can't do the §3.4 / §4 "does it
> look right" checks without data. Recommended: clear B1–B3 first (≈15 min), then
> build Part 4 on top of real DC data.

---

## 1. What's done

### Repo & infra
- Git on `main`; pushed to **public** repo <https://github.com/ntangborn/comic-avails>.
- `gh` CLI 2.96.0 installed + authenticated as `ntangborn`.
- Dev server: `npm run dev` → Next.js 16.2.10 (Turbopack) on
  <http://localhost:3000> (background task may not survive a shutdown — just
  re-run `npm run dev`).
- Chrome extension connected (browser automation available). Note: it needs
  per-site permission — comicreleases.com wasn't granted, so browser fetches of
  it hang; use WebSearch or a UA'd `fetch` instead.

### Database schema — DONE, **not yet applied**
`supabase/migrations/20260703120000_init_comic_avails_schema.sql` — PRD §6.1:
11 tables, enums, RLS (public read on catalog; owner-only on pull_lists/
pull_list_items/subscriptions; backend-only on distributors/publisher_distributor/
ingest_runs), `foc_date` index, dedup unique constraint (`NULLS NOT DISTINCT`),
dated `publisher_distributor` incl. Oni's 2026-08-01 switch, and seeds
(10 publishers, 3 distributors, §4.1 mapping).

### DC ingest script — DONE, **not yet run**
`scripts/ingest-dc.ts` (run: `npx tsx scripts/ingest-dc.ts <url>`). Fetch →
cheerio strip → `claude-sonnet-4-6` structured-output extraction (chunk+merge) →
validate → upsert (creates/links series+creators, records `ingest_runs`) →
summary. Typechecks clean. Deps installed: `@anthropic-ai/sdk`,
`@supabase/supabase-js`, `cheerio`, `tsx`.

### Solicitation source registry — DONE (new this session)
`scripts/lib/sources.ts` — typed registry for all 10 publishers: DB slug,
comicreleases URL slug, distributor, **current verified solicit URL**, fallbacks,
notes. Current window = **September 2026**. DC/Marvel/Image/Boom/Dark Horse/
Dynamite/IDW/Mad Cave verified; Titan (Sept unconfirmed) and Oni (coverage gap
post-PRH) flagged. Includes `monthlyIndexUrl()` for going-forward discovery.

### Integration plan — DONE (new this session)
`docs/plans/2026-07-03-solicit-ingestion-integration.md` — refactor to
`ingest-core` + generic `ingest.ts <slug>`, index-scraping discovery, Vercel-cron
automation, risks (403 bot-block, Oni gap), phased rollout, open decisions.

---

## 2. Blockers (must clear before Part 3 runs → before Part 4 has data)

### B1 — `.env.local` keys still misconfigured  ⛔
`SUPABASE_SECRET_KEY` and `ANTHROPIC_API_KEY` both still hold the *publishable*
key value (`sb_publishable_...`). Fix:
- `ANTHROPIC_API_KEY=sk-ant-...` (console.anthropic.com → API Keys)
- `SUPABASE_SECRET_KEY=sb_secret_...` (Supabase → Settings → API Keys → Secret
  keys → Create secret key, name `ingest-scripts`)

As-is: Anthropic 401, and Supabase writes denied by RLS.

### B2 — Migration not applied to Supabase
Paste `supabase/migrations/20260703120000_init_comic_avails_schema.sql` into the
Supabase SQL Editor and Run (build guide §2.2). Project:
`grydjxsgsyxprmkobzfj.supabase.co`.

### B3 — Unverified: does comicreleases.com 403 the script's `fetch`?
WebFetch got HTTP 403 (Cloudflare). The ingest script uses a browser-like UA and
may be fine — **but this is unproven.** The very first ingest run answers it. If
403'd: rotate a realistic UA / add backoff / fall back to a headless-browser
fetch or alternate aggregator. **This gates scaling to all 10 publishers.**

---

## 3. First-run expectations (DC)

Healthy: ~150–300 extracted, 90%+ upserted, a few sensible rejects. Note: the
ingest currently **rejects rows missing foc_date or street_date** (strict reading
of "dates must parse / foc before street") — if real DC rows are wrongly
rejected for a single missing date, that's Decision D4 (loosen it). After a run,
do the §3.4 trust check (10 items hand-checked against LoCG or the solicit page)
before cloning to other publishers.

---

## 4. Part 4 — the app (resume point)

Two build-guide prompts, run in order once DC data exists:

**4.1 Catalog UI** (PRD §5.1–5.2): default `/` = "This Week's FOC" (items with
`foc_date` in next 7 days, grouped by foc_date then publisher, cover placeholder,
series+issue, price, street date, FOC countdown); collapse variants to A-cover +
"+N variants" expander; filters (publisher multi-select, format, search over
series/title/creators — `ilike`/`pg_trgm`); `/week/[date]` by street-date week;
item detail page; pull list with Add buttons, `/pull-list` grouped by FOC with
qty steppers + remove, persisted to Supabase (single hardcoded user for now —
auth later). Dark, clean, data-dense, mobile-friendly.

**4.2 Print/export** (PRD §5.3): `/pull-list/print` B&W print-CSS layout (header:
customer/shop/date; grouped by FOC then publisher; columns item code/series/
issue/variant/price/street date/qty; ~25–30 rows/page); Download CSV; Download
PDF (server-rendered); Print button.

> ⚠️ **Modified Next.js.** `AGENTS.md` says this is NOT stock Next.js — read
> `node_modules/next/dist/docs/` before writing any App Router / route / data
> code. APIs and conventions may differ from training data. This matters most in
> Part 4, which is all Next.js.

Part 4 gate = PRD Phase 2 "usable by Nick": add ~15 items, print the pull list,
judge the printout as a shop owner would.

---

## 5. Outstanding decisions

### Schema / ingest (from prior handoff)
- **D1** Repo visibility — public now; flip to private via
  `gh repo edit ntangborn/comic-avails --visibility private` if desired.
- **D2** `distributors` / `publisher_distributor` — currently backend-only (RLS,
  no policy). Add public-read if the client must read the distributor mapping.
- **D3** `publisher_distributor` seed `effective_from` dates — partly approximate
  (2026-01-01 baseline); Oni's 2026-08-01 is exact. Verify/refine.
- **D4** Ingest rejects rows missing a foc/street date. Confirm strict, or loosen
  to keep single-date rows.
- **D5** Migration tool — Supabase CLI (`supabase db push`, not installed) vs.
  dashboard SQL editor.

### Ingestion integration (from the plan, §7)
- **P1** Generic `ingest.ts <slug>` (recommended) vs. per-publisher scripts.
- **P2** comicreleases-only vs. multi-source with fallbacks (wire Oni's now).
- **P3** Oni post-PRH long-term source — spike PRH catalog / Title API.
- **P4** Discovery cadence — weekly cron.
- **P5** Ingest newest month only vs. all currently-posted future months
  (recommend all).

---

## 6. Reference
- Schema: `supabase/migrations/20260703120000_init_comic_avails_schema.sql`
- DC ingest: `scripts/ingest-dc.ts`
- Source registry: `scripts/lib/sources.ts`
- Ingestion plan: `docs/plans/2026-07-03-solicit-ingestion-integration.md`
- PRD: `comic-avails-prd.md` · Build guide: `comic-avails-build-guide.md`
- Team/agent conventions: `CLAUDE.md`, `AGENTS.md`

## 7. Not yet committed
The migration, ingest script, `scripts/lib/sources.ts`, and the `docs/` files are
on disk but **not committed**. Consider committing before shutdown so nothing is
lost (`.env.local` stays gitignored). Nothing has been pushed since the initial
commit.
